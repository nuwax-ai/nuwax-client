# 意图：一条脚本进入 Nuwax 开发、构建和发布状态

- 日期：2026-09-26
- 状态：已接受（用户在本任务确认并请求实施）

## 问题

首次检出需手动初始化源码、安装依赖、准备运行资源和商业打包配置。已有开发入口重复安装/构建，部分资源准备会覆盖开发源码；本地打包缺少 CI 的商业身份及前端载荷。

## 预期结果

开发者与 agent 从外层入口执行 dev、frontend:build、pack、sub:update、release、doctor。必要准备自动完成，二次运行复用有效缓存，失败有明确阶段与续跑方式。

## 受影响的能力面

外层 Node 编排、前端构建、三子模块管理、本地商业包、现有 CI 发布编排、开发文档。

## 约束

- 从 release/v1.0.x 建隔离特性支，保留共享检出的并行改动。
- 本地 dev/pack 默认消费 nuwax-dist pin；source 模式用于当前前端改动。
- nuwax 的客户端版本线取消 dist 跟踪；网页 test 部署迁移后续处理。
- 正式发布编排现有 CI，phase 1 保留源码现场重建与 provenance；仅修正 smoke 旧断言。
- nuwax-dist public/main；保留仍被客户端引用的提交。
- Windows 原生命令行可用；系统工具与 SimplySign 手机认证为明确前置。
- --no-commit 全链不提交/推送；所有推送不强推。

## 开放问题

无产品决策待确认。真实平台验收与远端权限根据实施证据记录。
