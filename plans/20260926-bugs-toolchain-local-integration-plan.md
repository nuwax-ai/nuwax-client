# 客户端 Bug 与工具链本地集成

## 范围与基线

- 从工具链 `1e1c9304` 创建独立工作树，保持原 PR #11 分支不变。
- 顺序合入客户端 `726793ce`、`63181189`、`85a1b04c`、`c2ba72bb`、`6ba6757f` 和 file-server 配置 `50ab2083`。
- 基座固定 `f7e3f9319cf31eb03a9906e4a1eb7aa7096c0cd2`，前端固定 `f7bbe57e65bcfc49d6df88a4741ab815b1686b6c`。
- 使用新前端已完成生产构建的 dist 创建独立本地产物提交，更新源码/产物双 pin。源码不跟踪 dist。
- 仅前端及既有桌面客户端；不修改 Java、移动端，不推送、不发布、不改原 PR，不停止共享进程。

## 实施与验证

1. 独立克隆三个子仓库，不复用可写源码工作树；资源与依赖可采用本机文件系统复制保留隔离。
2. 合入客户端修复。loopback 保留新产物目录解析与生命周期串行队列，测试同时覆盖二者。
3. 校验 dist 版本戳及文件哈希，保留产物仓 README 和 Git 元数据，独立提交产物及外层双 pin。
4. 检查 overlay 同步、基座默认/staged/commit 字节纯净，脚本测试及完整商业测试；运行商业主进程/renderer 构建。
5. 核验 file-server 资源版本 1.5.3、资源 commit 与 npm fallback 配置一致。工具链生成 Computer Use 资源只在此隔离基座本地 exclude，禁止暂存进基座。
6. 记录本地源/产物/基座 SHA 及未推送边界。打包/真实 GUI 仍需独立验收，不因端口冲突停止共享服务。

## 风险

- 旧产物 `54268bf` 的戳为 `3bab563d54`，不能与新前端源码组合。
- 默认 `sub:update` 会推送产物；显式本地源码 SHA 又受远端可达性校验，因此本次只手工生成本地双 pin。
- file-server bundled source 声明仍跟随 main；本轮记录实际资源 commit，不能据此承诺未来 main 重建版本恒定。
- 历史验收报告描述旧基线；集成结果另记报告，保留历史证据。
