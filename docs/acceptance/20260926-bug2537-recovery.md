# #2537 加载切换与恢复验证（2026-09-26）

## 基线与隔离
- 外层：951aa59750491984ae8d767de9d11b3c3755c9bc，分支 codex/bug2537-20260926。
- 基座：52bd898f97d0a22e2d677a1c4bffab8fbeac2cf5，独立 clone/shared object store、独立文件检出，无修改原工作区。
- 商业 source 在 overlay；基座 gitlink 不变。历史 loopback 173f722a 尚未入本基线；webview recovery 070d51e9/6ede5093 对象在当前对象库不可得，按当前商业代码补修。

## 已复现与修复
1. 延迟 start → 用户改 direct → refresh：基线 race 测试 failed，最终 running=true，句柄未释放。修复统一 ensure/stop/refresh 生命周期队列，并在 start/镜像 await 后重新检查是否启用，关闭迟到句柄并清请求 capability/ticket origin。
2. 并发 ensure：基线启动两次；修复后一份 live handle，排队 stop 关闭一次。
3. 旧商业 HostWebview 遇真实主 frame -102 只 stopped，仍无恢复面板；新实现主文档失败/renderer gone/配置解析失败显示本地重试，按钮重建 guest。子 frame/-3 ERR_ABORTED 不当作主页面失败。失败诊断只保留 origin/path 和代码，不存 query/fragment/userinfo。

## 已跑验证
| 层 | 命令/方法 | 结果 |
|---|---|---|
| 定向 | vitest loopbackGateway/index.test.ts + webviewLoadFailure.test.ts | 18 passed |
| 商业全量 | npm run test:commercial | 139 files passed + 1 skipped，1724 passed / 18 skipped |
| 社区全量 | npm run base:test（此隔离基座清 overlay） | 121 files passed + 1 skipped，1478 passed / 18 skipped |
| 编译 | npm run build:renderer | 通过（chunk-size 存量 warning） |
| 纯净与同步 | overlay:check、check:pin、git diff --check | 通过 |
| 类型 | tsc --noEmit 社区与商业两轨 | 均有存量错误；本次 3 个源码和 helper 测试路径无错误，不能宣称全量 typecheck 绿 |
| 真实 Electron | node scripts/acceptance/webview-load-recovery.cjs，darwin Electron 40.8.2，独立 userData，无账号、仅 loopback | WEBVIEW_LOAD_RECOVERY_FIXTURE_OK |

依赖准备：独立 node_modules 目录以 symlink 共享 pnpm 已安装依赖，补齐已存在的 i18next/react-i18next 链接；复制原工作区 mcp-proxy-ts 生成资源到隔离副本。首次商业 suite 因缺该资源 59 个 mcp 用例失败，补齐后全绿；首次 renderer build 缺 i18next 链接，补齐后通过。未修改依赖 manifest/lockfile。

### 真实 webview fixture 证据
- 先关闭临时端口：真实 did-fail-load 主 frame -102，显示失败面板并截图。
- 同端口起服务，点击恢复按钮：guest id 变化，真实 document 成功加载；子 iframe -312 不覆盖成功页。
- 对当前 guest forcefullyCrashRenderer：真实 render-process-gone reason=killed，显示恢复面板；点击后新 guest 成功。
- settings IPC 受控拒绝：结束等待、显示重试，再次成功。
- 负对照：将组件临时还原 951aa597 商业版本，同 fixture 的真实 -102 后 10 秒没有失败面板，明确 failed。最终同步新 overlay 再跑成功。
- 工件目录：[本轮证据](/Users/apple/workspace/bug2537-recovery-evidence-20260926/evidence.json)，包含 screenshots、两轨日志、编译/类型日志及 fixture 正负对照日志。

## 尚未完成的现场/交付层
- 无 1.0.25 原用户现场 guest Console/Network、崩溃记录，不能把已证实的生命周期竞态认定为原现场唯一根因。
- 未构建或运行新安装包；未验 Windows 交互桌面、macOS 已安装包、真实业务后端 cookie/登录。当前 fixture 是真实 Electron + 修改后的组件源码，服务/鉴权接口被替代为本地固定配置。
- 未推送、部署、打 tag、发包或修改禅道。建议工单保留激活，补齐原场景包级复验后再判断关闭。
