# Nuwax 客户端发布验收记录模板

每个候选 tag 复制一份并填写；未执行写“未验证”，失败写证据与归属，不把构建成功计作真机通过。

| 项目 | 记录 |
|---|---|
| tag / 根仓 SHA | |
| 基座 / 前端 SHA | |
| CI 双轨、前端、脚本与静态门禁 run URL | |
| 五平台 `build-manifest-*.json` 来源一致 | |
| `release-provenance.json` 与安装包 SHA256 | |
| macOS 签名、公证和 Gatekeeper 结果 | |
| Windows EXE 校验：beta 未签名包来源哈希 / stable 签名与来源 | |
| S3/OSS 同步 run、beta/stable 指针版本 | |

## 安装包验收

| 场景 | macOS arm64 | macOS x64 | Windows x64 | Linux x64/arm64 | 证据 |
|---|---|---|---|---|---|
| 干净安装、启动与登录页 | | | | | |
| 正式版和 beta 默认生产环境；更新通道独立可切换；设置切域后无旧域会话 | | | | | |
| 登录、设备注册、服务启动；登出停服 | | | | | |
| 企业域 A→B→A，旧会话与服务失效 | | | | | |
| 外部网页无法调用宿主桥，受信任页面的桥能力正常 | | | | | |
| 本机权限首次授权、拒绝、重启和升级后的状态 | | | | | |
| 旧版本升级、下载、安装、重启及状态恢复 | | | | | |
| 同机社区版隔离、退出无业务进程残留 | | | | | |

Windows 安装验证须在交互桌面会话进行。每格填写“通过 / 失败 / 阻塞 / 未验证”，证据应指向同一安装包 SHA。真实账号或后端不可用时记录阻塞，不替代为 mock 结果。
