# 女娲Nuwax 1.0.31（beta）

修复 Windows 覆盖安装失败。

- 升级前检查旧安装目录中末尾带点或空格的异常文件名；先移到安装目录外的恢复文件夹，并记录原路径与备份路径，再调用旧卸载器。
- 修正安装器关闭应用进程树时使用的进程 ID，避免向 `taskkill` 传入无效过滤条件。
- 延续 1.0.30 及更早 beta 的功能与修复；现有客户端数据目录和 beta 更新通道保持不变。

Windows 安装包由 CI 构建，未签名。恢复文件夹仅在发现异常文件时创建，命名为 `Nuwax-installer-recovery-*`，位置在旧安装目录的同级目录。
